# ========== 房主本机环境（复制为 .env 后按需改） ==========
#
# --- 推荐：单隧道 + Vite 代理（仓库根已有 .env 时默认已开）---
#
#   VITE_BGIO_THROUGH_VITE=true
#
# 本机三个步骤：
#   1) 终端 A：npm run server
#   2) 终端 B：npm run dev
#   3) 终端 C：cloudflared tunnel --url http://127.0.0.1:5173
#      （或 ngrok http 5173）
#
# 房主请用穿透得到的 https 地址打开页面，再点「邀请玩家」；外网玩家打开链接即可。
#
# 若房主坚持用 http://localhost:5173 打开，请取消注释并填公网前端地址，邀请链接才会给外网用：
#   VITE_PUBLIC_APP_ORIGIN=https://xxxx.trycloudflare.com
#
# --- 备选：双隧道（注释掉 VITE_BGIO_THROUGH_VITE，改用游戏服公网根地址）---
#
#   VITE_BGIO_SERVER=https://你的游戏服隧道根地址
#
# 若浏览器报 CORS，在运行 npm run server 的终端执行（示例）：
#   PowerShell:  $env:BGIO_EXTRA_ORIGINS="https://你的前端隧道Origin"
#   bash:       BGIO_EXTRA_ORIGINS=https://你的前端隧道Origin npm run server
#
# 游戏服端口（默认 8000）：
#   PowerShell:  $env:PORT=8000; npm run server
#
# 若「追加模组/剧本/NPC → 保存到工程」报 HTTP 404，多为游戏服仍为旧进程未载入最新 host.ts，
#   请先停掉原来的 npm run server 再重新执行一次。
